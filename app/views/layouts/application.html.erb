<!DOCTYPE html>
<html>
<head>
    <title>Awfulix</title><%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %><%= javascript_include_tag 'application', 'data-turbolinks-track' => true %><%= stylesheet_link_tag 'application', 'https://fonts.googleapis.com/css?family=Open+Sans:400&subset=latin', media: 'all', 'data-turbolinks-track' => true %><%= csrf_meta_tags %>
</head>
<body>
    <!-- Google font  -->
    <link href='https://fonts.googleapis.com/css?family=Press+Start+2P' rel=
    'stylesheet' type='text/css'><!-- new nav -->
    <nav class="navbar navbar-light navbar-fixed-top" style=
    "background-color: #92A06A;">
        <div class="container">
            <div class="row">
                <ul class="nav navbar-nav">
                    <li>
                        <a class="navbar-brand" href="/"><b>Awfulix</b></a>
                    </li>
                    <li>
                    <%=link_to "Movies", movies_path %></li><%if current_user %>
                    <li>
                    <%=link_to "Profile", user_path(current_user) %></li><%end %>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <%if current_user %>
                      <li><%=link_to "Logout", logout_path%></li>
                    <%else %>
                      <li><%=link_to "Login", login_path%></li>
                      <li><%=link_to "Sign Up", new_user_path%></li>
                    <%end%>
                </ul>
                <form class="form-inline pull-xs-right">
                    <%= form_tag movies_path, {method: 'get'} do %>
                    <%= text_field_tag :search, params[:search], class:'form-control', placeholder:"Search movies"%>
                    <%= submit_tag "Search", name: nil, class: "btn btn-secondary-outline" %>
                  <% end %>
                </form>
            </div>
        </div>
    </nav><!-- new nav -->
    <%= yield %>
</body>
</html>
